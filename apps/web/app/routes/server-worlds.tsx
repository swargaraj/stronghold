import { ServerSectionPlaceholder } from "~/components/server-section-placeholder";

export default function ServerWorlds() {
  return (
    <ServerSectionPlaceholder
      description="World lifecycle actions, imports, and terrain management will live here."
      title="Worlds"
    />
  );
}
