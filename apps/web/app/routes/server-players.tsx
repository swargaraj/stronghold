import { ServerSectionPlaceholder } from "~/components/server-section-placeholder";

export default function ServerPlayers() {
  return (
    <ServerSectionPlaceholder
      description="Online players, player actions, and moderation tools will live here."
      title="Players"
    />
  );
}
