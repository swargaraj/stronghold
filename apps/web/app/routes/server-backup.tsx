import { ServerSectionPlaceholder } from "~/components/server-section-placeholder";

export default function ServerBackup() {
  return (
    <ServerSectionPlaceholder
      description="Snapshots, restore points, and scheduled backup actions will live here."
      title="Backup"
    />
  );
}
