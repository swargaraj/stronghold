import { ServerSectionPlaceholder } from "~/components/server-section-placeholder";

export default function ServerSoftware() {
  return (
    <ServerSectionPlaceholder
      description="Version channels, software selection, and upgrade workflows will live here."
      title="Software"
    />
  );
}
