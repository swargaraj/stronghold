import { ServerSectionPlaceholder } from "~/components/server-section-placeholder";

export default function ServerSettings() {
  return (
    <ServerSectionPlaceholder
      description="Server configuration, lifecycle controls, and environment settings will live here."
      title="Settings"
    />
  );
}
