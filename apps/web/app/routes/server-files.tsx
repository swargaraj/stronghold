import { ServerSectionPlaceholder } from "~/components/server-section-placeholder";

export default function ServerFiles() {
  return (
    <ServerSectionPlaceholder
      description="File browsing, editing, uploads, and server data management will live here."
      title="Files"
    />
  );
}
